apiVersion: v1
kind: Service
metadata:
  name: logguard-app
  namespace: logguard
spec:
  type: NodePort
  selector:
    app: logguard-app
  ports:
    - port: 5001
      targetPort: 5001
      nodePort: 30001
